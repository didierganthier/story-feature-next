import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: 
{
  domains: ['cdn-icons-png.flaticon.com', 'www.alerterouge.org', 'images.unsplash.com', 'plus.unsplash.com']
}
};

export default nextConfig;
